# Superseded instrumented pilot

This attempt was stopped during fit trials to correct the scan-pollution setup: each scan now explicitly warms the hot point-query set after symbol-search scans and records its own pre-scan window. These early results must not be mixed with the final `results/comparison` matrix. Any missing final JSON represents an aborted exploratory trial, not a pass. The live-heap/removal instrumentation itself was unchanged.
